# misc
.DS_Store

# Monorepo (Yarn workspaces + Turborepo)
node_modules/
.turbo/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# CI PR metrics (generated in Actions / local CI dry-runs)
/ci-metrics/
**/test-results.json

# APNs keys — never commit
**/*.p8
.vercel

# Firebase Android client config (use google-services.json.example)
mobile/android/app/google-services.json
