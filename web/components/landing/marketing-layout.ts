/** Outer viewport gutters — matches {@link Header} wrapper. */
export const marketingGutterClass = 'px-3 sm:px-6 lg:px-8';

/** Centered page column — matches header bar max width. */
export const marketingContainerClass = 'mx-auto w-full max-w-7xl';

/** Inner horizontal inset — matches header card padding. */
export const marketingInsetClass = 'px-3 sm:px-4';

/** Section content aligned to header inner edges (logo ↔ actions). */
export const marketingContentClass = `${marketingContainerClass} ${marketingInsetClass}`;
