/** Shared pill controls under the embedded audio player on the note screen. */
export const RECORDING_DETAIL_METADATA_CHIP_TOUCHABLE_CLASS =
  'max-w-full shrink flex-row items-center gap-1.5 rounded-full px-3 py-2';

export const RECORDING_DETAIL_METADATA_CHIP_TEXT_CLASS =
  'shrink text-[14px] font-semibold leading-[18px]';

export const RECORDING_DETAIL_METADATA_CHIP_ICON_SIZE = 16;
export const RECORDING_DETAIL_METADATA_CHIP_CHEVRON_SIZE = 16;
