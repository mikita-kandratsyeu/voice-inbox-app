export const startRecordingBackgroundService = async (): Promise<void> => {};

export const stopRecordingBackgroundService = async (): Promise<void> => {};

export const isBackgroundServiceRunning = (): boolean => false;
