/** Readable column width for note document reading and source editing on tablet. */
export const NOTE_DOCUMENT_CONTENT_MAX_WIDTH = 1152;

/** Horizontal inset for note document body on tablet (phone uses screen-local values). */
export const NOTE_DOCUMENT_TABLET_HORIZONTAL_PADDING = 24;
